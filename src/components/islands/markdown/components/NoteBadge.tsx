/**
 * ╔══════════════════════════════════════════════════════════════════════════════╗
 * ║  🟣 NoteBadge.tsx               "The Label"                                  ║
 * ╠══════════════════════════════════════════════════════════════════════════════╣
 * ║  👤 I am the note type badge. I appear in the top-left corner to indicate   ║
 * ║     that this is a markdown note. I show when selected or hovered.          ║
 * ╚══════════════════════════════════════════════════════════════════════════════╝
 * 
 * 💬 WHO IS IN MY SOCIAL CIRCLE?
 * 
 *      ┌─────────────────────────────────────────────────────────────────┐
 *      │                        MY NEIGHBORS                              │
 *      ├─────────────────────────────────────────────────────────────────┤
 *      │                                                                  │
 *      │   ┌─────────────┐      ┌──────────────┐                        │
 *      │   │ useSelection│─────▶│      ME      │                        │
 *      │   │  useHover   │      │    Badge     │                        │
 *      │   └─────────────┘      └──────────────┘                        │
 *      │                                                                  │
 *      └─────────────────────────────────────────────────────────────────┘
 * 
 * 📦 PROPS I RECEIVE:
 * ┌─────────────────────┬──────────────────────────────────────────────────────┐
 * │ isVisible           │ Whether to show the badge                            │
 * │ isSelected          │ Whether note is selected (affects color)             │
 * └─────────────────────┴──────────────────────────────────────────────────────┘
 * 
 * @module markdown/components/NoteBadge
 */

import React from 'react';

interface NoteBadgeProps {
    /** Whether to show the badge */
    isVisible: boolean;
    /** Whether note is selected (affects styling) */
    isSelected: boolean;
}

/**
 * Note type badge component
 */
export const NoteBadge = React.memo(function NoteBadge({
    isVisible,
    isSelected,
}: NoteBadgeProps) {
    if (!isVisible) return null;

    const isDark = typeof document !== 'undefined' && 
        document.documentElement.getAttribute('data-theme') === 'dark';

    return (
        <div
            style={{
                position: 'absolute',
                top: '8px',
                left: '12px',
                fontSize: '10px',
                fontWeight: 600,
                color: isSelected ? '#818cf8' : (isDark ? '#a1a1aa' : '#6b7280'),
                textTransform: 'uppercase',
                letterSpacing: '0.5px',
                pointerEvents: 'none',
                opacity: 0.9,
                zIndex: 1000,
            }}
        >
            📝 Markdown
        </div>
    );
});
